KC_BOOTSTRAP_ADMIN_USERNAME=admin
KC_BOOTSTRAP_ADMIN_PASSWORD=changeme

# PostgreSQL Database (for Keycloak internal use)
KEYCLOAK_DB_NAME=keycloak
KEYCLOAK_DB_USER=keycloak
KEYCLOAK_DB_PASSWORD=changeme

# Public Hostname (for OAuth redirects and OIDC discovery)
# Used by browsers when redirected to login
KC_HOSTNAME=auth2.5tritti.de
